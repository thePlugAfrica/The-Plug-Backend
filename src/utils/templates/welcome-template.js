const welcomeTemplate = (firstname) => {
  return `
        <div style="text-align: center;">
        <h3>Welcome to the Family, ${firstname}</h3>
        <p>We're very excited to have you on board.</p>
        </div>
    `;
};

export default welcomeTemplate;
